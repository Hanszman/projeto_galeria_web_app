export class ConfigClass { // Classe aonde tem o método que retorna a url da api.
    static getUrlApi(){
        return 'http://localhost:3000'; // Url local da api desenvolvida no projeto rest-api. 
    }
}
